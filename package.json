{
  "name": "lemonjet",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ lemonjet",
    "create-local": "graph create --node http://localhost:8020/ lemonjet",
    "remove-local": "graph remove --node http://localhost:8020/ lemonjet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lemonjet",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.33.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
